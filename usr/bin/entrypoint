#!/bin/sh

PHINX=true

crond -b
nginx
php-fpm7 -F -R

phinx || PHINX=false
swagger || true

if [ "$PHINX" = true ] ; then
    phinx migrate
    if [ "$APP_ENV" != "prod" ] ; then
        phinx seed:run
    fi
fi
